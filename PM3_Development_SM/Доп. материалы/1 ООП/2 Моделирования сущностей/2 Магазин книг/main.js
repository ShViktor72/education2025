import { BookStore, Cart } from "./store.js";

const booksDiv = document.getElementById("books");
const cartDiv = document.getElementById("cart");

const store = new BookStore();
const cart = new Cart();

// TODO: вызвать методы для отображения книг и корзины
// store.render(booksDiv, cart);
// cart.render(cartDiv);
